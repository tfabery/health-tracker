<% if @user.foods.any? %>
  <%= sortable "name", "Sort By Name", "food" %>
  <%= sortable "calories", "Sort By Calories", "food" %>
  <% @foods.each do |food| %>
    <p><%= food.name %>: <%= food.calories %></p>
  <% end %>
<% end %>

<p>Total: <%= @user.food_calories %> calories consumed</p>
